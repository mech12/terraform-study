server:
  ingress:
    enabled: true
    ingressClassName: "nginx"
    hosts:
      - argowf.localtest

controller:
  singleNamespace: true
  postgresql:
    host: argowf-rw.cnpg.svc
    port: 5432
    database: postgres
    tableName: argo_workflows
    userNameSecret:
      name: argowf-superuser
      key: username
    passwordSecret:
      name: argowf-superuser
      key: password

useDefaultArtifactRepo: true
artifactRepository:
  archiveLogs: true
  s3:
    endpoint: standalone-minio.argowf.svc:9000
    insecure: false
    bucket: argo-workflow
    accessKeySecret:
      name: argwf-user
      key: accesskey
    secretKeySecret:
      name: argwf-user
      key: secretkey
